{# Auto-generated by Homepage Section Builder v2. #}
{# section_type: about #}
<section class="home-section home-section-haqqimizda">
  <div class="row align-items-center g-4">
    <div class="col-12 col-md-6">
      {% if fields.headline %}
        <h2>{{ fields.headline }}</h2>
      {% endif %}

      {% if fields.description %}
        <p>{{ fields.description|nl2br }}</p>
      {% endif %}
    </div>

    {% if fields.image %}
      <div class="col-12 col-md-6">
        <img src="image/{{ fields.image }}" alt="{{ fields.headline ?: 'Haqqımızda' }}" class="img-fluid w-100"/>
      </div>
    {% endif %}
  </div>
</section>

{#
array elementlerini gormek ucun ucun:
<pre>{{ custom_fields|json_encode }}</pre>
#}